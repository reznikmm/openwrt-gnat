diff --git a/toolchain/gcc/Config.in b/toolchain/gcc/Config.in
index 34c597c..2479f3c 100644
--- a/toolchain/gcc/Config.in
+++ b/toolchain/gcc/Config.in
@@ -73,3 +73,10 @@ config INSTALL_GCCGO
 	default n
 	help
 	    Build/install GNU gccgo compiler ?
+
+config INSTALL_ADA
+	bool
+	prompt "Build/install Ada compiler?" if TOOLCHAINOPTS
+	default n
+	help
+	    Build/install GNU Ada compiler? Working native ada compiler required.
diff --git a/toolchain/gcc/common.mk b/toolchain/gcc/common.mk
index c6e01b5..46ee37c 100644
--- a/toolchain/gcc/common.mk
+++ b/toolchain/gcc/common.mk
@@ -69,9 +69,13 @@ HOST_STAMP_CONFIGURED:=$(GCC_BUILD_DIR)/.configured
 HOST_STAMP_INSTALLED:=$(STAGING_DIR_HOST)/stamp/.gcc_$(GCC_VARIANT)_installed
 
 SEP:=,
-TARGET_LANGUAGES:="c,c++$(if $(CONFIG_INSTALL_LIBGCJ),$(SEP)java)$(if $(CONFIG_INSTALL_GFORTRAN),$(SEP)fortran)$(if $(CONFIG_INSTALL_GCCGO),$(SEP)go)"
+TARGET_LANGUAGES:="c,c++$(if $(CONFIG_INSTALL_LIBGCJ),$(SEP)java)$(if $(CONFIG_INSTALL_GFORTRAN),$(SEP)fortran)$(if $(CONFIG_INSTALL_GCCGO),$(SEP)go)$(if $(CONFIG_INSTALL_ADA),$(SEP)ada)"
 
-TAR_OPTIONS += --exclude='gcc/testsuite/*' --exclude=gcc/ada/*.ad*
+TAR_OPTIONS += --exclude='gcc/testsuite/*'
+
+ifndef CONFIG_INSTALL_ADA
+  TAR_OPTIONS += --exclude=gcc/ada/*.ad*
+endif
 
 ifndef CONFIG_INSTALL_LIBGCJ
   TAR_OPTIONS += --exclude=libjava
